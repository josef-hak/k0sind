# A 1 control-plane + 2 worker k0s cluster.
# Usage: k0sind create cluster --config examples/multi-node.yaml --name multi
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
  - role: worker
  - role: worker
